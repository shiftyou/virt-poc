apiVersion: v1
kind: Service
metadata:
  name: node-exporter-service
  namespace: poc-node-exporter
  labels:
    monitor: metrics
spec:
  selector:
    monitor: metrics
  ports:
    - name: metrics
      port: 9100
      targetPort: 9100
      protocol: TCP
